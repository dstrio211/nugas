const cache='nugas-v1';self.addEventListener('install',e=>e.waitUntil(caches.open(cache).then(c=>c.addAll(['/','/manifest.webmanifest','/icon.svg']))));self.addEventListener('fetch',e=>{if(e.request.method==='GET')e.respondWith(fetch(e.request).catch(()=>caches.match(e.request)));});
